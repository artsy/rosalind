machine:
  ruby:
    version: 2.3.0
dependencies:
  pre:
    - gem install bundler --version 1.13.6
test:
  pre:
    - bundle exec rubocop --rails
    - npm run lint
  override:
    - bundle exec rspec
    - npm run test
  post:
    - bundle exec danger
